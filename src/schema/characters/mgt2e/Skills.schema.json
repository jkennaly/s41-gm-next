{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name or title of the skill."
      },
      "level": {
        "type": "string",
        "description": "The proficiency level or rating of the skill."
      },
      "description": {
        "type": "string",
        "description": "A brief description or explanation of the skill."
      },
      "category": {
        "type": "string",
        "description": "The category or group to which the skill belongs."
      },
      "parentSkill": {
        "type": "string",
        "description": "The parent skill if applicable, indicating a hierarchical relationship."
      },
      "specializations": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional specializations associated with the skill."
      },
      "SourceReference": {
        "type": "object",
        "description": "The reference to the source material or book where the augmentation is described or detailed."
      }
    },
    "required": ["name", "level", "description", "category"]
  }
  